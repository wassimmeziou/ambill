{% extends 'base.html.twig' %}

{% block title %}Inventaire{% endblock %}

{% block body %}
    <h1>Inventaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inventaire.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('inventaire_index') }}">back to list</a>

    <a href="{{ path('inventaire_edit', {'id': inventaire.id}) }}">edit</a>

    {{ include('inventaire/_delete_form.html.twig') }}
{% endblock %}
