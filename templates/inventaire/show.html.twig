{% extends 'base.html.twig' %}

{% block title %}Inventaire{% endblock %}

{% block body %}
    <h1>Inventaire</h1>

    <table class="table">
        <tbody>
            <tr>
       
               
              
                   
            </tr>
            <tr>
                     <th>Id</th>
                <td>{{ inventaire.id }}</td>
            </tr>
            <tr>
                <th>reference</th>
                  <td>{{ inventaire.reference }}</td>
            </tr>
            <tr>
               <th>Depot</th>
                <td>{{ inventaire.depot }}</td>
            </tr>
            <tr>
                 <th>date</th>
                <td>{{ inventaire.dateInv? inventaire.dateInv|date('Y-m-d') : ''  }}</td>

            </tr>
        </tbody>
    </table>

    <a href="{{ path('inventaire_index') }}" style="color:#00AAFF"><button class="btn btn-light custombutton" style="float:right;margin-left:5px;" type="button"/><i class="lnr lnr-arrow-left-circle"></i>Retourner</a>

    <a href="{{ path('inventaire_edit', {'id': inventaire.id}) }}" style="color:#fff"><button class="btn btn-primary custombutton" style="float:right" type="button"/><i class="fa fa-check-circle"></i>Modifier</a>


    {{ include('inventaire/_delete_form.html.twig') }}
{% endblock %}
