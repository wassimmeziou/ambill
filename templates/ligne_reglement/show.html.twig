{% extends 'base.html.twig' %}

{% block title %}LigneReglement{% endblock %}

{% block body %}
    <h1>LigneReglement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_reglement.id }}</td>
            </tr>
            <tr>
                <th>PrixRestant</th>
                <td>{{ ligne_reglement.prixRestant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ligne_reglement_index') }}">back to list</a>

    <a href="{{ path('ligne_reglement_edit', {'id': ligne_reglement.id}) }}">edit</a>

    {{ include('ligne_reglement/_delete_form.html.twig') }}
{% endblock %}
