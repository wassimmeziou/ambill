<form method="post" action="{{ path('stock_voitures_delete', {'id': stock_voiture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock_voiture.id) }}">
    <button class="btn">Delete</button>
</form>
