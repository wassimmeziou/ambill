{% extends 'base.html.twig' %}

{% block title %}StockVoitures{% endblock %}

{% block body %}
    <h1>StockVoitures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock_voiture.id }}</td>
            </tr>
            <tr>
                <th>QuantiteStockVoiture</th>
                <td>{{ stock_voiture.quantiteStockVoiture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stock_voitures_index') }}">back to list</a>

    <a href="{{ path('stock_voitures_edit', {'id': stock_voiture.id}) }}">edit</a>

    {{ include('stock_voitures/_delete_form.html.twig') }}
{% endblock %}
