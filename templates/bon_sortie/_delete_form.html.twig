<form method="post" action="{{ path('bon_sortie_delete', {'id': bon_sortie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bon_sortie.id) }}">
    <button class="btn">Delete</button>
</form>
