{% extends 'base.html.twig' %}

{% block title %}LigneFacture index{% endblock %}

{% block body %}
    <h1>LigneFacture {{ facture.client}}  &&  {{ facture.commercial}}  </h1>
    <h2><div style="float:right;color:blue">{{ facture.dateSaisie ? facture.dateSaisie|date('Y-m-d') : '' }} </div></h2>


    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Article</th>
                <th>Quantité</th>
                
                <th>Prix UHT</th>
                <th>TVA</th>
                <th>Prix TT</th>
                <th>Remise</th>
                <th>Prix Finale</th>
                {# <th>actions</th> #}
            </tr>
        </thead>
        <tbody>
        {% for ligne_facture in ligne_factures %}
            <tr>
                <td>{{ ligne_facture.id }}</td>
                <td>{{ ligne_facture.article }}</td>
                <td>{{ ligne_facture.qte }}</td>
              
                <td>{{ ligne_facture.prixUHT }}</td>
                <td>{{ ligne_facture.tva }}</td>
                <td>{{ ligne_facture.prixTT }}</td>
                <td>{{ ligne_facture.remise }}</td>
                <td>({{ ligne_facture.qte }}*{{ ligne_facture.prixTT }}/{{ ligne_facture.remise }})</td>
                <td>
                    {# <a href="{{ path('ligne_facture_show', {'id': ligne_facture.id}) }}">show</a> #}
                    {# <a href="{{ path('ligne_facture_edit', {'id': ligne_facture.id}) }}">edit</a> #}
                </td>
            </tr>
              <tr>
                <td colspan="8"> {{ facture.prixTotal}}</td>
            </tr>
           
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('ligne_facture_new') }}">Create new</a> #}
{% endblock %}
