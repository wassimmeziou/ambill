{% extends 'base.html.twig' %}

{% block title %}Edit Visite{% endblock %}

{% block body %}
    <h1>Edit Visite</h1>

    {{ include('visite/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('visite_index') }}">back to list</a>

    {{ include('visite/_delete_form.html.twig') }}
{% endblock %}
