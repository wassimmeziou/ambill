{% extends 'base.html.twig' %}

{% block title %}LigneCommand{% endblock %}

{% block body %}
    <h1>LigneCommand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_command.id }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ ligne_command.qte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ligne_command_index') }}">back to list</a>

    <a href="{{ path('ligne_command_edit', {'id': ligne_command.id}) }}">edit</a>

    {{ include('ligne_command/_delete_form.html.twig') }}
{% endblock %}
