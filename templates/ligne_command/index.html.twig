{% extends 'base.html.twig' %}

{% block title %}LigneCommand index{% endblock %}

{% block body %}
    <h1>LigneCommand index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Article</th>
                <th>Quantité</th>
            </tr>
        </thead>
        <tbody>
        {% for ligne_command in ligne_commands %}
            <tr>
                <td>{{ ligne_command.id }}</td>
                <td>{{ ligne_command.article }}</td>
                <td>{{ ligne_command.qte }}</td>
         
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('ligne_command_new') }}">Create new</a> #}
{% endblock %}
